export default interface IControlSettings {
  moveLeft: string;
  moveRight: string;
  moveDown: string;
  drop: string;
  rotateClockwise: string;
  rotateAntiClockwise: string;
  start: string;
  pause: string;
}
