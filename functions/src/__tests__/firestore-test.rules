rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // allow full access to setup test data
    match /{document=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}