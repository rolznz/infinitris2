import { I } from '@models/blockLayouts/Tetrominoes';
import { LayoutSet } from '../Layout';

const longsOnly: LayoutSet = {
  id: 'longsOnly',
  name: 'I shapes only',
  layouts: { I },
};

export default longsOnly;
