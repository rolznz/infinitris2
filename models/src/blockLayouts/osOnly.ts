import { O } from '@models/blockLayouts/Tetrominoes';
import Layout, { LayoutSet } from '../Layout';

const osOnly: LayoutSet = {
  id: 'osOnly',
  name: 'Square shapes Only',
  layouts: { O },
};

export default osOnly;
