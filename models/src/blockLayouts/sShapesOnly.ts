import { S } from '@models/blockLayouts/Tetrominoes';
import Layout, { LayoutSet } from '../Layout';

const sShapesOnly: LayoutSet = {
  id: 'sShapesOnly',
  name: 'S shapes only',
  layouts: { S },
};

export default sShapesOnly;
