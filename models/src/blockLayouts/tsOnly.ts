import { T } from '@models/blockLayouts/Tetrominoes';
import Layout, { LayoutSet } from '../Layout';

const tsOnly: LayoutSet = {
  id: 'tsOnly',
  name: 'T shapes only',
  layouts: { T },
};

export default tsOnly;
